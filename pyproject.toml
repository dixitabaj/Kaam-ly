[project]
name = "kaam-ly"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.127.0",
    "motor>=3.7.1",
    "pymongo>=4.15.5",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.40.0",
]

[tool.uv.workspace]
members = [
    "worker",
]
